---
name: react
description: React/TypeScript frontend — loads React architecture and patterns
globs:
  - '**/*.tsx'
  - '**/*.jsx'
  - '**/features/**/*.ts'
  - '**/components/**/*.ts'
  - '**/hooks/**/*.ts'
  - '**/pages/**/*.ts'
  - '**/app/**/*.tsx'
skills:
  - react-best-practices
  - bulletproof-architecture
  - frontend-code-style
  - react-query-patterns
---

## React Context

Fires for React/TypeScript frontend files (components, hooks, pages, features).

Recommended skills:

- `react-best-practices` — component, hook and state patterns
- `bulletproof-architecture` — feature-based project layout
- `frontend-code-style` — formatting and naming conventions
- `react-query-patterns` — data fetching and cache management
