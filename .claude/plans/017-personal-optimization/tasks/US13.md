---
us: US13
title: Wire prompt-engineer into tech-plan — every US is born an execution prompt
wave: W4
depends_on: [US7, US9]
tdd_mode: optional
estimate: S
status: draft
absorbs_decision: USs are prompts because the Lead executes them (Commandment VIII on the plan itself)
---

# US13 — prompt-engineer cableado en el flow

## Execution prompt (Phase 3 input)

**Task**: Wire the US-as-execution-prompt discipline permanently: (1) `tech-plan/SKILL.md` Step 10 requires the "Execution prompt (Phase 3 input)" block per US (template already updated in 017) and adds a quality pass — score each US block against the prompt-engineer 5-criteria rubric before closing Phase 2; (2) `flow.md` mentions the requirement at the Phase 2 description; (3) `build/SKILL.md` declares the block as its primary input when present.
**Context**: User insight (2026-06-10): the Lead executes USs inline, so a US IS a prompt — Arch-H quality criteria (context, goal, constraints, deliverable, verification) apply. prompt-engineer Context 3 (review delegation prompt) is the rubric source; `plans/templates/tasks.template.md` already carries the block.
**Constraints**: Deterministic wiring, not auto-trigger reliance (known lesson: skill wiring over auto-trigger). Don't duplicate the rubric — reference prompt-engineer's scoring-criteria.md. Runs AFTER US7 (flow.md) and US9 (phase skills) to avoid file collisions. English.
**Deliverable**: tech-plan + build + flow.md wired; the requirement traceable in each.
**Verify**: `grep -n "Execution prompt" .claude/skills/tech-plan/SKILL.md .claude/skills/build/SKILL.md .claude/commands/flow.md` → present in all 3; next /flow feature produces USs with the block without being asked.
**Ask first**: nothing — decisions locked.

## User story

- **As a**: Oriol
- **I want**: every future US written as a scored execution prompt
- **So that**: build inputs are systematically high-quality, not artisanal

## Acceptance criteria

- **AC1**: Given tech-plan after the change, when producing USs, then each carries the execution-prompt block and a rubric pass before gate 2→3.
- **AC2**: Given build, when reading a US with the block, then it treats the block as the primary instruction.
- **AC3**: Given the wiring, when grepping the 3 files, then the requirement is explicit (deterministic, not auto-trigger).

## Commandments cubiertos

| # | Cómo |
|---|---|
| VIII | Meta-prompting óptimo aplicado al propio plan |
| IV | Rubric pass = gate de calidad sobre los inputs de build |

## Verificación post-implementación

- Greps de AC3 + smoke en la siguiente feature.
