---
us: US12
title: Activation + observation hooks — UserPromptSubmit Skill(x) injection, InstructionsLoaded log
wave: W3
depends_on: [US8]
tdd_mode: forced
estimate: S
status: closed
closed: 2026-06-10
implemented: 2026-06-10
absorbs_decision: verify event availability before building
---

# US12 — Hooks de activación y observación

## Execution prompt (Phase 3 input)

**Task**: (a) Create `.claude/hooks/skill-activation.ts` (UserPromptSubmit): match prompt keywords/paths against the skills list and inject an explicit `Skill(<name>)` instruction via system-reminder — community-proven that explicit tool-call instructions fire and vague reminders don't. (b) Create an `InstructionsLoaded` hook logging which instruction files loaded per session — closes the "verify load layer" lesson.

**Context**: This is the never-built `prompt-enrichment.ts`, now validated by ≥3 independent community implementations (gist umputun, scottspence.com, claudefa.st). `skill-advisor` stays as the human-ratified layer; this is the deterministic layer underneath. Known caveat: UserPromptSubmit reliability gap early-session/post-compaction (issue #17277) — best-effort, not sole gate.

**Constraints**: VERIFY FIRST: `InstructionsLoaded` event existence in current CC docs/changelog [Probable — agent-reported]; if absent, build only (a) and record the finding. Injection must be ≤5 lines per prompt (no context bloat); matching reads skill names/keywords from disk, no hardcoded list that rots. tdd forced — red→green. Registration in settings.json after US8.

**Deliverable**: hook file(s) + tests green + settings registration + 3-line note in docs/system-inventory.md.

**Verify**: `bun test ./.claude/hooks/` green; simulated prompt "valida este plan" produces injection naming `drillme`; prompt with no match produces zero output.

**Ask first**: nothing — decisions are locked; only the InstructionsLoaded existence check can re-scope (documented fallback).

## User story

- **As a**: Oriol
- **I want**: skills to activate deterministically when relevant and proof of which instruction layers loaded
- **So that**: the "parece que sí pero luego no se llaman" problem dies at the root

## Acceptance criteria

- **AC1**: Given a prompt matching a skill's triggers, when UserPromptSubmit fires, then an explicit `Skill(<name>)` instruction (≤5 lines) is injected; no match → no output.
- **AC2**: Given the event verification, when InstructionsLoaded exists, then the observation hook logs loaded instruction files per session; when it doesn't, the finding is recorded and scope reduces to (a).
- **AC3**: Given both hooks, when tested, then red→green coverage of match, no-match, and malformed-payload paths.

## Commandments cubiertos

| # | Cómo |
|---|---|
| V | La skill correcta carga cuando toca — contexto relevante usado |
| IX | InstructionsLoaded = observabilidad de capas de carga |
| II | Evento verificado antes de construir |

## Verificación post-implementación

- `bun test ./.claude/hooks/` green; smoke con prompt real en sesión siguiente.
