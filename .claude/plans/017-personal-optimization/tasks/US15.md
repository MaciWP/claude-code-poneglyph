---
us: US15
title: Memory curation — prune stale always-injected MEMORY.md entries
wave: W4
depends_on: []
tdd_mode: skip: curation of memory files, no testable behavior
estimate: S
status: closed
closed: 2026-06-10
implemented: 2026-06-10
absorbs_decision: urgent gap found in scope review — same class as CLAUDE.md bloat
---

# US15 — Curación de memoria persistente

## Execution prompt (Phase 3 input)

**Task**: Audit the 21 entries in the auto-memory (`~/.claude/projects/.../memory/MEMORY.md` + files): per entry decide KEEP (still true + behavior-changing) / UPDATE (true but stale wording or superseded references) / DELETE (wrong, superseded, or one-off). Known candidates: "Skill propagation research" (superseded by `_research-skill-activation-2026-06-09.md`), "Stress-test thesis A-conditional" (2026-05-27, decisions since absorbed), entries referencing components this feature deletes/renames (verify each against post-017 reality).
**Context**: MEMORY.md index loads EVERY session — stale entries are the same problem as CLAUDE.md bloat but in the memory layer. The memory protocol says: update rather than duplicate, delete wrong memories.
**Constraints**: Feedback-type memories (user corrections) have the highest bar to delete — they encode durable preferences; verify against current behavior before touching. Each DELETE listed to the user before applying (they're his accumulated instructions to me). Run AFTER W1-W3 land (entries referencing changed components resolve then).
**Deliverable**: curated memory dir + one-line summary table (entry → verdict → reason).
**Verify**: every MEMORY.md index line points at an existing file; no entry contradicts post-017 repo reality.
**Ask first**: the DELETE list before applying.

## User story

- **As a**: Oriol
- **I want**: the always-injected memory to be as truthful as the rest of the system
- **So that**: every session starts from current reality, not May's

## Acceptance criteria

- **AC1**: Given the audit table, when reviewed, then all 21 entries carry verdict + reason and DELETEs were user-approved.
- **AC2**: Given post-curation memory, when cross-checked against the post-017 repo, then no entry references dead components as live.

## Commandments cubiertos

| # | Cómo |
|---|---|
| II | La memoria deja de afirmar cosas superadas |
| VII | Menos inyección muerta por sesión |
| X | La capa de memoria entra en el mantenimiento del sistema |

## Verificación post-implementación

- Tabla de veredictos completa; índice ↔ ficheros consistente.
