---
us: US10
title: Abstraer skills/commands de binora a la capa global (pr-comments, commit/pr-text, review-pr core)
wave: WB
depends_on: []
tdd_mode: optional
estimate: M
status: closed
closed: 2026-08-05
---

# US10 — Abstracción binora → global (registro de cierre)

Ejecutada directamente desde la especificación del index (HU de Wave B sin
fichero previo — expandida aquí como registro al cerrar).

## Entregado

| Pieza | Destino | Decisiones |
|---|---|---|
| `pr-conventional-comments` | Skill global (`.claude/skills/`) + `examples/comment-examples.md` verbatim | Frontmatter re-canonizado desde el schema pre-poneglyph (`type`, `for_agents`, `context: fork`, `activation.keywords` → description es-ES + Keywords); body intacto (battle-tested). Typo `h#` de la fuente corregido en NUESTRA copia — pendiente avisar a binora-backend |
| `/commit-message` | Command global | Generalizado: submodules sin nombres hardcoded, patrón de ticket con fallback, estilo detectado del git log. En repos binora su versión de proyecto tiene precedencia |
| `/pr-description` | Command global | Generalizado: rama base detectada (dev→main), sección de migraciones condicional al stack, categorías por stack |
| review-pr núcleo común | `project-onboard/templates/review-pr.template.md` | NO command global (chocaría con `/review` y `/code-review` nativos — escalera rung 1). El template lleva el hardening medido: Phase 0 arg-validation (3 misparse "pull request `necesito`") + scope discipline "Fuera del diff" (2 overreach); criterios = placeholder por stack |

## Verificación

- Symlinks: commands y skill visibles en `~/.claude/` ✅
- Hook: "prepara los comentarios de la review" → hint `Skill(pr-conventional-comments)` ✅
- Suite: 143/143 ✅
- Despliegue a repos binora: run posterior de `project-onboard` (canal fijado por el plan — poneglyph no edita repos ajenos).
