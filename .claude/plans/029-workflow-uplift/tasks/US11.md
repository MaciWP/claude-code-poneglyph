---
us: US11
title: Open-plans accionable — protocolo de primer turno + ruta de script corregida
wave: WA
depends_on: []
tdd_mode: forced
estimate: S
status: closed
closed: 2026-08-05
note: Red→green honrado (T2.4/T2.5 fallaron con el error predicho antes del impl). Verificado en vivo desde binora-backend — el recordatorio nuevo emite ruta $HOME ejecutable cross-repo (probada — lista los 5 planes de ESE repo) + protocolo de acción de primer turno (ofrecer resume/archivar UNA vez, no insistir). Meta-detalle — el propio cierre de esta US tropezó con la clase de bug que arregla (ruta relativa desde cwd equivocado).
---

# US11 — Open-plans: de recordatorio pasivo a protocolo de acción

## Execution prompt (Phase 3 input)

**Task**: Convert the SessionStart open-plans reminder from a passive listing into an actionable first-turn protocol, and fix its broken script reference.
**Context (verified 2026-08-05)**: The hook fires correctly (run live from binora-backend: lists 5 open plans) but nobody acts on it — 10/14 lifecycles remain open since jun/jul. Its text tells the user to run `bun .claude/scripts/flow-state.ts status` — a RELATIVE path that does not exist in work repos; the synced copy DOES exist at `~/.claude/scripts/flow-state.ts` (verified, 11.5KB, Jul 8). Hook source: `.claude/hooks/session-start-plans.ts` (delegates to `openPlansReminder` in `post-compact.ts`).
**Constraints**: Two changes, both in the emitted TEXT (the detection logic works): (1) script path → `bun $HOME/.claude/scripts/flow-state.ts status`; (2) the reminder ends with an ACTION INSTRUCTION to the Lead, not just information — pattern: "En el primer turno de esta sesión, si la tarea del usuario no es urgente-incompatible, ofrece en UNA línea: cerrar el más antiguo (`/flow --resume <slug>` → critic/retro pendiente) o archivarlo; si el usuario ignora la oferta, no insistir el resto de la sesión" (anti-ceremony: one offer, once). Keep the reminder ≤10 lines. Hook logic edits carry red→green tests (`.claude/hooks/__tests__/`), extending existing post-compact/session-start tests.
**Deliverable**: `post-compact.ts` (openPlansReminder text) + test updates; sync-claude run.
**Verify**: `bun test ./.claude/hooks/` green; live smoke: run the hook from binora-backend cwd → new text with $HOME path + action instruction.
**Ask first**: nothing — decisions locked (the "ofrecer una vez, no insistir" calibration viene de la evidencia de que hoy se ignora siempre).

## ⚡ Quick reference

| Campo | Valor |
|---|---|
| **Status** | 🟡 draft |
| **Wave** | WA |
| **Depends on** | none |
| **Blocks** | [US12] |
| **Files touched** | `.claude/hooks/post-compact.ts` · `.claude/hooks/__tests__/*` |
| **TDD-mode** | forced — texto emitido por hook, red→green |
| **Estimate** | S |
| **Cómo arrancar** | Leer `openPlansReminder` en post-compact.ts + su test actual |

## User story

- **As a**: Oriol
- **I want**: que al abrir sesión con lifecycles a medias se me ofrezca cerrarlos en una línea accionable (y que el comando que me dan funcione)
- **So that**: los 10 planes pagados-y-no-cobrados se cierran en vez de acumularse

## Acceptance criteria

- **AC1**: Given el hook, when emite recordatorio, then la ruta del script es `$HOME/.claude/scripts/flow-state.ts` (ejecutable desde cualquier repo) — test red→green.
- **AC2**: Given el recordatorio, when emitido, then contiene la instrucción de acción de primer turno (ofrecer resume/archivar UNA vez) — test.
- **AC3**: Given una sesión sin planes abiertos, when arranca, then cero output (comportamiento actual preservado) — test existente sigue green.
- **AC4**: Smoke en vivo desde binora-backend: texto nuevo visible.

## Commandments cubiertos

| # | Cómo |
|---|---|
| IV | El gate de cierre deja de depender de que alguien se acuerde |
| IX | Repara la referencia rota (clase sync-trap, memoria 025) |

## Verificación post-implementación

- `bun test ./.claude/hooks/` green; smoke en vivo desde binora-backend.
