---
us: US16
title: Topología del workspace Bjumper — mapa y navegación orgánica (extensión worktrees-bjumper)
wave: WA
depends_on: []
tdd_mode: optional
estimate: S
status: closed
closed: 2026-08-05
note: Re-verificación pre-escritura corrigió 2 asunciones del plan — (1) envs actuales jrv-1077/1081 llevan los 3 repos hermanos, jrv-1031 vacío (huérfano); (2) el caveat era parcialmente falso — .claude/ SÍ viaja a worktrees (trackeado en git); lo que no viaja es el estado SIN COMMITEAR (state.json vivos) ni transcripts (per-cwd) → regla práctica escrita en la reference. Smoke real desde worktrees/jrv-1077/binora-frontend — las 3 primitivas de descubrimiento funcionan. Keywords de navegación añadidas a la description (hook las descubre — hint verificado). Colisión menor detectada — best-of-n también matchea "worktree" (ruido, fichado para censo US14).
---

# US16 — Topología del workspace: repos ↔ worktrees ↔ docs

## Execution prompt (Phase 3 input)

**Task**: Extend the existing `worktrees-bjumper` skill with a workspace-topology reference so any session — started in a main repo, a worktree, or elsewhere — can locate and navigate the Bjumper workspace organically.
**Context (verified 2026-08-05)**: Workspace root: `/Users/oriol/Desktop/Bjumper/REPOSITORIOS/PYTHON`. Main repos: binora-backend, binora-frontend, binora-contract, binora-mcp, bjumper-worktrees (the CLI's own repo, with README.md, AGENTS.md, docs/, recipes/), plus legacy/others. Worktree envs: `<root>/worktrees/<env>/<repo>/` — active today: jrv-1031, jrv-1077, jrv-1081; infra dirs `_shared` and `_proxy` at the same level. Discovery primitives verified: `git worktree list` from a main repo enumerates its worktrees with branch; the CLI has status/doctor commands (see skill references/cli-contract.md). The existing skill (206 lines + 3 references) covers the CLI but topology only in one line. User requirement: "que poneglyph sepa que existe, cómo funciona y cómo buscar estos documentos/repos para navegar de forma orgánica".
**Constraints**: Single owner (Cmd IX): the topology lives in the `worktrees-bjumper` skill as a new reference `references/workspace-topology.md` + a pointer/summary block in its SKILL.md — NOT a new skill, NOT always-loaded prose (the skill's es-ES activation surface already matches "worktree", "binora", "sandbox", "entorno local" — verify keywords catch "dónde está el repo", "navega a", "en qué worktree estoy"; add if missing, with test). Content of the reference (all verified before writing — re-run the discovery commands, don't copy this prompt blindly): (1) workspace map (root, main repos with one-line purpose, worktrees layout, `_shared`/`_proxy` roles); (2) discovery protocol: from a worktree → its main repo (`git worktree list` / path convention) and its env siblings (same `<env>` dir); from a main repo → its envs; from anywhere → `ls <root>/worktrees/`; (3) doc locations per repo (README, AGENTS.md, CLAUDE.md, docs/, recipes/) and which to read for what; (4) session-context caveat: transcripts/plans are per-cwd — a worktree session does NOT see the main repo's `.claude/plans` (explains lifecycle fragmentation observed). Keep the reference ≤120 lines; paths as literals so Glob/ls commands are copy-paste runnable.
**Deliverable**: `~/.claude/skills/worktrees-bjumper/references/workspace-topology.md` (via repo + sync) · SKILL.md pointer block · keyword additions in skill-activation.ts if gaps found (+ test).
**Verify**: from a worktree cwd (e.g. `worktrees/jrv-1077/binora-frontend`), following the reference locates: main repo, env siblings, CLI repo docs — smoke run of each discovery command; `bun test ./.claude/hooks/` green if keywords touched.
**Ask first**: nothing — decisions locked (extend existing skill; the user named the requirement explicitly).

## ⚡ Quick reference

| Campo | Valor |
|---|---|
| **Status** | 🟡 draft |
| **Wave** | WA (requisito previo declarado por el usuario) |
| **Depends on** | none |
| **Blocks** | soft-dep de US10 y US15 (ambas navegan estas capas) |
| **Files touched** | `.claude/skills/worktrees-bjumper/references/workspace-topology.md` · `SKILL.md` (bloque puntero) · `.claude/hooks/skill-activation.ts` (solo si faltan keywords, + test) |
| **TDD-mode** | optional |
| **Estimate** | S |
| **Cómo arrancar** | Re-verificar topología con ls + git worktree list; leer SKILL.md actual para encajar el bloque sin duplicar |

## User story

- **As a**: Oriol
- **I want**: que poneglyph sepa que trabajamos con git worktrees y bjumper-worktrees, dónde vive todo y cómo encontrar repos/docs
- **So that**: cualquier sesión navega el workspace orgánicamente sin que yo le explique la estructura cada vez

## Acceptance criteria

- **AC1**: Given la reference, when leída, then contiene el mapa (root, repos principales, layout `worktrees/<env>/<repo>`, `_shared`/`_proxy`) con paths literales ejecutables.
- **AC2**: Given una sesión arrancada en un worktree, when sigue el protocolo de descubrimiento, then localiza su repo principal, sus hermanos de env y los docs del CLI (smoke verificado).
- **AC3**: Given el caveat de contexto, when leído, then explica que plans/transcripts son per-cwd (un worktree no ve `.claude/plans` del repo principal).
- **AC4**: Given prompts como "en qué worktree estoy" / "levanta el entorno de la JRV-X", when se emiten, then el hint de skill-activation propone `worktrees-bjumper` (keywords verificadas o añadidas con test).

## Commandments cubiertos

| # | Cómo |
|---|---|
| I | Contexto del workspace disponible antes de actuar en cualquier repo Bjumper |
| IX | Extiende el dueño existente (worktrees-bjumper) en vez de crear pieza paralela |

## Verificación post-implementación

- Smoke desde `worktrees/jrv-1077/binora-frontend`: los 3 descubrimientos funcionan.
- `bun test ./.claude/hooks/` green (si se tocan keywords).
