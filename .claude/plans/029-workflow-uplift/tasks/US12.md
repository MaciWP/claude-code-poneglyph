---
us: US12
title: Back-half re-enganche — cierre barato de lifecycles atascados + eliminación de modos
wave: WB
depends_on: [US11]
tdd_mode: forced
estimate: M
status: closed
closed: 2026-08-05
---

# US12 — Back-half re-enganche (registro de cierre)

Ejecutada desde la especificación del index. La ask-first (¿retro light?) produjo
una decisión del usuario MÁS amplia que redefinió la HU:

> **Decisión (2026-08-05)**: NO hay modos. `/flow` siempre es FULL —
> `--minimal/--standard/--full` eliminados. Lo que genuinamente no aplique lo
> salta el LEAD, con justificación explícita y aviso previo. La retro se salta
> solo si no hay nada que aprender — decisión del Lead, registrada.

## Entregado (TDD forced — red→green honrado)

| Pieza | Detalle |
|---|---|
| `flow-state.ts` +`retro-status` | Contrato: `approved` \| `pending` \| `skipped — <justificación ≥10 chars>`; el skip sin justificación se RECHAZA |
| `closeFeature` guard endurecido | **Bug cazado al leer el código**: hardcodeaba `retro_status: "approved"` al cerrar — estampaba retros que nunca corrieron. Ahora exige retro approved o skip justificado ANTES de cerrar, y PRESERVA el valor real |
| Tests | 7 nuevos (26/26 scripts green); los 2 existentes de closeFeature actualizados al contrato nuevo |
| `flow.md` | Banner MODES ELIMINATED (flags parsean pero se ignoran con aviso; tablas de modos = legacy hasta US17) + ruta de re-enganche en Step 5 Resume: fase 4/5 atascada → verdict → retro (o skip justificado) → close-feature en UNA sesión corta |
| `retro/SKILL.md` | Skip discipline: decisión del Lead, anunciada ANTES, registrada vía retro-status; el skip silencioso es mecánicamente imposible |

## Verificación

- Suites: scripts 26/26 · hooks 143/143 ✅
- Smoke en vivo (fixture): close-feature con retro null → RECHAZA ✅ · `retro-status "skipped"` sin justificación → RECHAZA ✅ · skip justificado + close-feature → cierra preservando el skip ✅
- Conductual (deferred por naturaleza): la primera resurrección real de uno de los 10 lifecycles abiertos de binora — la oferta de US11 + esta ruta se estrenan en la próxima sesión allí.

## Nota para US17

La reescritura de flow.md consolida: eliminar las tablas de modos legacy, el
argument-hint con flags, y el paso de triage — la doctrina ya es "always full +
skip justificado por fase".
