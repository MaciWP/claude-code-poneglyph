---
us: US10
title: Verificación final (AC1-AC9 + coherencia cross-file)
wave: W3
depends_on: [US1, US2, US3, US4, US5, US6, US7, US8, US9]
tdd_mode: optional
estimate: S
status: closed
---

# US10 — Verificación final 🔴

## ⚡ Quick reference

| Campo | Valor |
|---|---|
| **Status** | 🟡 draft |
| **Wave** | W3 (gate de verificación) |
| **Depends on** | [US1-US9] |
| **Blocks** | cierre Phase 3 → Phase 4 (critic) |
| **Files touched** | ninguno (Grep/Bash + quickfixes puntuales ≤2 files) |
| **TDD-mode** | optional |
| **Estimate** | S |
| **Cómo arrancar** | Ejecutar la matriz de Greps AC + `bun test` |
| **Decisión absorbida** | — |

## User story

- **As a**: Lead
- **I want**: verificar que el refactor cumple los 9 AC del spec y deja el sistema coherente
- **So that**: Phase 4 (critic) arranque sobre base limpia y "todo quede perfecto"

## Acceptance criteria

- **AC1** (spec AC1): `Grep -ri "context isolation|for isolation"` en `skills/ commands/ docs/ CLAUDE.md hooks/` → **0** como justificación de spawn (`plans/` exento).
- **AC2** (spec AC2): refs al criterio de spawn apuntan al árbol (US1); 0 umbrales divergentes.
- **AC3** (spec AC3): `Glob .claude/agents/{builder,reviewer,scout}.md` → vacío; `Grep` 0 refs vivas a ellos como spawn-target (excluyendo falsos positivos de la tabla de exentos).
- **AC4** (spec AC5): `Grep "planner" 05-team-mode.md` → 0 como agente.
- **AC5** (spec AC8): `bun test ./.claude/hooks/` → **verde**.
- **AC6** (spec AC9): wording version verificado ("v2.1.154+", #24316/#31977); memoria `independent-reviewer` reconciliada (panel-≥4).
- **AC7**: lectura cruzada — árbol (US1) ↔ todos los referenciadores coherentes; falsos positivos intactos.

## Files a crear / a modificar

| Path | Cambio |
|---|---|
| (ninguno) | Solo verificación; quickfix puntual ≤2 files si un Grep revela ref olvidada (si >2 → reabrir la HU dueña) |

## Workflow detallado

1. `Grep -ri "context isolation"` en superficie viva → 0.
2. `Grep -rn "→ builder|delegates to .builder|≥5 files OR architectural|subagent_type=.(builder|reviewer|scout)"` → 0 vivas.
3. `Glob .claude/agents/*.md` → builder/reviewer/scout ausentes.
4. `Grep "\b(builder|reviewer|scout)\b"` repo-wide; cruzar cada hit contra la tabla de exentos; 0 no-exentos vivos.
5. `Grep "planner" 05-team-mode.md` → 0; `Grep "panel de review"` doc-wiring → existe.
6. `bun test ./.claude/hooks/` → verde.
7. Lectura cruzada de coherencia. Reportar matriz AC1-AC9 (spec) → resultado para el gate de Phase 4.

## Commandments cubiertos

| # | Cómo |
|---|---|
| IV | Gate bloqueante antes de Phase 4 |
| II | Cada AC verificado con evidencia (Grep/Bash) |

## Verificación post-implementación

- Matriz AC1-AC9 (spec) → todos ✅ con evidencia de comando.

## Open questions

- Si un Grep revela zona no inventariada → quickfix (≤2 files) o reabrir HU dueña.
