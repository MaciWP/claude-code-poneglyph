---
description: "Transversal Socratic check — applies canonical 4-category catalog (location/approach/context/failure) + complementary patterns (5-whys, first principles, inversion) to any decision, plan, or doubt. Auto-detects active workflow phase if context provided."
argument-hint: "[contexto o pregunta]"
---

$ARGUMENTS

Invoke the `drillme` skill with the provided context. If no argument is given, the skill auto-detects active phase from `.claude/plans/{NNN}-{slug}/` artefacts or asks the user for an explicit brief.
