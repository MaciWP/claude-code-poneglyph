{
  "skills": ["typescript-patterns"],
  "query": "Build a type-safe event emitter that enforces event name and payload types at compile time",
  "expected_behavior": [
    "Uses mapped types or template literal types for event names",
    "Enforces payload type matching for each event",
    "Provides type inference for event handlers",
    "Uses interface over type where appropriate"
  ]
}
