{
  "skills": ["typescript-patterns"],
  "query": "Create a generic repository pattern with TypeScript that works with any entity type",
  "expected_behavior": [
    "Uses generics with proper constraints",
    "Defines clear interfaces for CRUD operations",
    "Uses discriminated unions for result types",
    "Avoids any type, uses unknown where needed"
  ]
}
