# {{PROJECT_NAME}}

<!-- Generated by project-onboard. HARD CAP: 100 lines. Per-line test:
     "would removing this cause mistakes?" If Claude can infer it from the
     code, it does NOT belong here. -->

{{ONE_PARAGRAPH_WHAT_THIS_REPO_IS — only what the dir listing doesn't show}}

## Commands

<!-- Verbatim from {{SOURCE_FILE}} — never invented. Only commands Claude
     can't guess (bespoke flags, env prereqs, non-obvious order). -->

| Action | Command |
|---|---|
| Test | `{{TEST_COMMAND}}` |
| Lint / typecheck | `{{LINT_COMMAND}}` |
| Build / run | `{{BUILD_COMMAND}}` |

## Verification

After each change run `{{TEST_COMMAND}}` — never report completed without it passing.
{{IF_NO_TESTS: No test suite exists today — smallest viable check: `{{FALLBACK_CHECK}}`. Flag this gap when work touches business logic.}}

## Style — deviations from defaults only

<!-- Rules that DIFFER from what the stack's conventions already imply. -->

- {{DEVIATION_1}}
- {{DEVIATION_2}}

## Gotchas

<!-- Things that bit someone: env quirks, ordering constraints, flaky areas. -->

- {{GOTCHA_1}}

## Project layer

- Test policy: `.claude/rules/test-policy.md` ({{LEVEL}})
{{IF_PATH_RULES: - Path rules: `.claude/rules/paths/*.md`}}
{{IF_PROJECT_SKILLS: - Project skills: `.claude/skills/*/SKILL.md`}}
