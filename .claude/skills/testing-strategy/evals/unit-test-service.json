{
  "skills": ["testing-strategy"],
  "query": "Write unit tests for a UserService that depends on a database repository and email service",
  "expected_behavior": [
    "Uses bun test framework",
    "Mocks database repository and email service",
    "Tests happy path and error cases",
    "Uses descriptive test names with describe/it blocks"
  ]
}
