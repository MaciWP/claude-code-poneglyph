{
  "skills": ["security-coding"],
  "query": "Implement JWT authentication with refresh tokens for an Elysia API",
  "expected_behavior": [
    "Uses Bun.password.hash with argon2id for password hashing",
    "Implements short-lived access tokens (15min) and long-lived refresh tokens",
    "Validates JWT with proper error handling",
    "Stores refresh tokens securely with httpOnly cookies"
  ]
}
