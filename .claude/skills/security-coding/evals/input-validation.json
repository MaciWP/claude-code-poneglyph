{
  "skills": ["security-coding"],
  "query": "Add input validation and sanitization to a user registration endpoint",
  "expected_behavior": [
    "Uses Zod schemas for input validation",
    "Sanitizes string inputs to prevent XSS",
    "Validates email format and uniqueness",
    "Returns structured validation error responses"
  ]
}
